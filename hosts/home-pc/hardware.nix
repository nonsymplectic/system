# Hardware configuration for home-pc
# Hardware features (nvidia, bluetooth) now configured via features/* in default.nix
{...}: {
  # Host-specific hardware configuration goes here
  # (Currently empty - all hardware enabled via features)
}
